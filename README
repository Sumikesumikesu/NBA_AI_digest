Автоматизации для генерации дайджеста новостей НБА (демо). Программа использует API GigaChat через langchain.
Установите все зависимости (можно использовать install_requirements.sh), затем запустите main.py (или run_NBA_digest.sh).

Прочитать заголовки новостей-исходников можно в фалйе news.txt.
Сам дайджест будет распечатан в терминал и записан в файл digest.txt
